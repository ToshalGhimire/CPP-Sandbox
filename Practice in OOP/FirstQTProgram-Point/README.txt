My first QT GUI Program

it is a 2d graph and where user enters coordinates and point object appears on graph

users can then make lines that connects the two (or more points) and 
also return distance between two point objects 